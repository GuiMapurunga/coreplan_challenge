<%= form_with(model: computer_ram) do |form| %>
  <% if computer_ram.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(computer_ram.errors.count, "error") %> prohibited this computer_ram from being saved:</h2>

      <ul>
        <% computer_ram.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
