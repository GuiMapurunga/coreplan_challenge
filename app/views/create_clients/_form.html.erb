<%= form_with(model: create_client) do |form| %>
  <% if create_client.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(create_client.errors.count, "error") %> prohibited this create_client from being saved:</h2>

      <ul>
        <% create_client.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
