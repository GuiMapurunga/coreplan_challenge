<%= form_with(model: create_computer_ram) do |form| %>
  <% if create_computer_ram.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(create_computer_ram.errors.count, "error") %> prohibited this create_computer_ram from being saved:</h2>

      <ul>
        <% create_computer_ram.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
