<%= form_with(model: @ram, local: true) do |form| %>
  <% if @ram.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ram.errors.count, "error") %> prohibited this@ram from being saved:</h2>
      <ul>
        <% @ram.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :capacity %>
    <%= form.number_field :capacity %>
  </div>

   <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
